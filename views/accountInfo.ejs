<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Profile</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f4f4f4;
      }

      .navbar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: #2c2c2c;
        padding: 10px 20px;
        color: #fff;
      }

      .navbar-left {
        display: flex;
        align-items: center;
      }

      .navbar-brand {
        font-size: 24px;
        font-weight: bold;
        color: #fff;
        text-decoration: none;
        margin-right: 20px;
      }

      .navbar-link {
        font-size: 18px;
        color: #fff;
        text-decoration: none;
        margin-right: 20px;
      }

      .navbar-link:hover {
        text-decoration: underline;
      }

      .navbar-right {
        position: relative;
      }

      .dropdown {
        position: relative;
        display: inline-block;
      }

      .dropdown-btn {
        background: none;
        color: #fff;
        border: none;
        font-size: 18px;
        cursor: pointer;
      }

      .dropdown-content {
        display: none;
        position: absolute;
        right: 0;
        background-color: #fff;
        color: #000;
        min-width: 160px;
        box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2);
        z-index: 1;
      }

      .dropdown-content a {
        color: black;
        padding: 12px 16px;
        text-decoration: none;
        display: block;
      }

      .dropdown-content a:hover {
        background-color: #f1f1f1;
      }

      .dropdown:hover .dropdown-content {
        display: block;
      }

      .dropdown:hover .dropdown-btn {
        background-color: #3e8e41;
      }

      .profile-container {
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        width: 350px;
        padding: 20px;
        text-align: center;
        margin: 40px auto;
      }

      .profile-header {
        margin-bottom: 20px;
      }

      .profile-img {
        width: 100px;
        height: 100px;
        border-radius: 50%;
        object-fit: cover;
      }

      .profile-name {
        font-size: 24px;
        margin-top: 10px;
      }

      .profile-details {
        text-align: left;
        margin-bottom: 20px;
      }

      .profile-detail {
        margin-bottom: 10px;
      }

      .profile-detail label {
        font-weight: bold;
        display: block;
      }

      .profile-detail span {
        display: block;
        margin-top: 5px;
      }

      .profile-edit {
        text-align: center;
      }

      .edit-link {
        display: inline-block;
        padding: 10px 20px;
        color: #fff;
        background-color: #007bff;
        border-radius: 4px;
        text-decoration: none;
      }

      .edit-link:hover {
        background-color: #0056b3;
      }
    </style>
  </head>
  <body>
    <nav class="navbar">
      <div class="navbar-left">
        <a href="#" class="navbar-brand">Blogify</a>
        <a href="/home" class="navbar-link">Home</a>
        <a href="/blog" class="navbar-link">Add Blog</a>
        <div class="dropdown">
          <button class="dropdown-btn"><%= user.fullName %> ▼</button>
          <div class="dropdown-content">
            <a href="/account-info">Account</a>
            <a href="/logout">Logout</a>
          </div>
        </div>
      </div>
    </nav>

    <div class="profile-container">
      <div class="profile-header">
        <img
          src="<%= user.profileImageUrl %>"
          alt="User Image"
          class="profile-img"
        />
        <h1 class="profile-name"><%= user.fullName %></h1>
      </div>
      <div class="profile-details">
        <div class="profile-detail">
          <label>Name:</label>
          <span><%= user.fullName %></span>
        </div>
        <div class="profile-detail">
          <label>Email:</label>
          <span><%= user.email %></span>
        </div>
        <div class="profile-detail">
          <label>Password:</label>
          <span>********</span>
        </div>
      </div>
      <div class="profile-edit">
        <a href="/edit-profile" class="edit-link">Edit Details</a>
      </div>
    </div>
  </body>
</html>
